import '/core/base/base_view_model.dart';
import '/core/logger/logger.dart';

class SignupViewModel extends AppBaseViewModel {
  final Logger _logger;

  SignupViewModel(this._logger);

  onSignUpTap() {
    // appNavigator.navigateTo(to)
  }
}
